# Glossary

One row per word the project uses in a specific sense. The **Meaning** column paraphrases
the document that owns the word; the **Defined in** column is that document, and it wins
whenever the two disagree. A row without a source does not belong here — this file points,
it does not define. Add a row when a word had to be explained twice; `tools/backlog/build.py
--check` reports a row whose link resolves to nothing, and an axis token that has no row.

The **Met in** column says where a reader meets the word, in one or more of four
tokens. `code` — an identifier or a comment in the source. `rules` — a document the
project works by or a file its owner edits by hand: CLAUDE.md, the board standard, an
ADR, `privacy_rules.json`, `config.ini`. `screen` — text the application puts in front
of a person. `wire` — a DPTP field, command or value that crosses between nodes. Two
tokens on one row are two places one word is met, never two meanings: the row holds one
meaning, and a word that means two things gets two rows, or a sentence naming the other
sense the way **node** and **shell tier** do. The tokens are read against the sense the
row defines and no other — the axis called **knowledge** is `code, rules` because the
Knowledge panel on screen is the other word.

The naming rule that this file serves is in [CLAUDE.md](../CLAUDE.md#vocabulary--read-the-projects-words-before-naming-anything):
read the vocabulary before naming anything new, build names from the words the model already
has, grep before you coin.

## Direction, decisions, and the board

| Term | Met in | Meaning | Defined in |
|---|---|---|---|
| **axis** | code, rules | The direction a decision or a board entry serves, one word from a five-word vocabulary; one or two per record, three is a smell. The counter per axis of work finished and never seen working is the project's health signal. | [BACKLOG_FORMAT.md §4a](BACKLOG_FORMAT.md#4a-the-one-field-that-was-added-anyway--axis) |
| **collective** | code, rules | Axis: from personal to collective — P2P, groups, identity, signatures, history. VISION's first vector. | [BACKLOG_FORMAT.md §4a](BACKLOG_FORMAT.md#4a-the-one-field-that-was-added-anyway--axis), [VISION.md — Direction](../VISION.md#direction) |
| **knowledge** | code, rules | Axis: from passive to collaborative — memory, retrieval, the agent that works on it. VISION's second vector. | [BACKLOG_FORMAT.md §4a](BACKLOG_FORMAT.md#4a-the-one-field-that-was-added-anyway--axis), [VISION.md — Direction](../VISION.md#direction) |
| **network** | code, rules | Axis: from local to networked — local inference, compute, cost, the gateway. VISION's third vector. | [BACKLOG_FORMAT.md §4a](BACKLOG_FORMAT.md#4a-the-one-field-that-was-added-anyway--axis), [VISION.md — Direction](../VISION.md#direction) |
| **honesty** | code, rules | Axis: that the project's own numbers mean something — eval, CI, the board and its gates. Added by the board standard as a loop the project cannot be honest without; not one of VISION's vectors. | [BACKLOG_FORMAT.md §4a](BACKLOG_FORMAT.md#4a-the-one-field-that-was-added-anyway--axis) |
| **reach** | code, rules | Axis: that somebody outside can find and use this — docs, releases, distribution. VISION's fourth vector, "from one practice to many", counted as a trace of adoption rather than as a number. | [BACKLOG_FORMAT.md §4a](BACKLOG_FORMAT.md#4a-the-one-field-that-was-added-anyway--axis), [VISION.md — Direction](../VISION.md#direction) |
| **constraint (C1–C10)** | rules | One of VISION's ten non-negotiable boundaries, each either enforced by architecture or consciously defended. | [VISION.md — Foundation Constraints](../VISION.md#foundation-constraints) |
| **ADR** | code, rules | Architecture Decision Record: a permanent record of a structural decision future contributors would otherwise re-litigate, one file per decision under `docs/decisions/`, with YAML front matter the checker validates. | [decisions/TEMPLATE.md](decisions/TEMPLATE.md), [protocol-13-public.md §5](../protocol-13-public.md#5-decision-artifact-system-3-layers) |
| **decision status** | code, rules | One token from `proposed → accepted → implemented → deprecated → superseded-by-NNN` (or `rejected`); an accepted or implemented decision must carry an axis. | [decisions/TEMPLATE.md — Status Lifecycle](decisions/TEMPLATE.md#status-lifecycle) |
| **board / backlog** | code, rules | The project's task list as a plain markdown file: one task per `###` heading with an envelope `(PRIORITY, STATUS, DATE — origin)`, prose below. It is a dated synthesis of what to check, never a statement that something is still true. | [BACKLOG_FORMAT.md §1](BACKLOG_FORMAT.md#1-the-entry) |
| **entry** | code, rules | One board task: a `SCREAMING-KEBAB` name that states a claim, an envelope, and free prose (Observed / Inferred / First step). The name is the handle everything else cross-references. | [BACKLOG_FORMAT.md §1](BACKLOG_FORMAT.md#1-the-entry) |
| **origin** | code, rules | Who raised an entry and when, in their words when there are words; the one field that may carry Russian. | [BACKLOG_FORMAT.md §1](BACKLOG_FORMAT.md#1-the-entry), [§8](BACKLOG_FORMAT.md#8-the-check) |
| **section / status** | code, rules | The `##` heading an entry sits under is its status (`OPEN`, `IN PROGRESS`, `DONE — AWAITING OBSERVATION`, `BLOCKED ON DECISION`, `BACKLOG`, `IDEAS`); the heading duplicates it so a bare chunk still says what it is. | [BACKLOG_FORMAT.md §2](BACKLOG_FORMAT.md#2-status-lives-in-two-places-on-purpose) |
| **observation shelf** | code, rules | The `DONE — AWAITING OBSERVATION` section: code written and not yet seen working in production. An entry leaves it only by recording an observation. | [BACKLOG_FORMAT.md §2](BACKLOG_FORMAT.md#2-status-lives-in-two-places-on-purpose) |
| **observation debt** | code, rules | The number of entries on the observation shelf, printed per axis in the generated ROADMAP block. | [ROADMAP.md — Status by axis](../ROADMAP.md#status-by-axis) |
| **resolution** | code, rules | Why an entry left the working file: `fixed`, `disproved`, `moot`, `superseded`, `duplicate`, `wontfix`; each requires its own kind of evidence in the closure line. | [BACKLOG_FORMAT.md §3](BACKLOG_FORMAT.md#3-closing-an-entry) |
| **closure line** | code, rules | `**Closed:** S<session> · YYYY-MM-DD · <resolution> · <evidence>`, written when an entry moves to `backlog_closed.md`; never backfilled onto old entries. | [BACKLOG_FORMAT.md §3](BACKLOG_FORMAT.md#3-closing-an-entry) |
| **session identifier** | code, rules | `S<YYYY-MM-DD>.<N>`: the UTC date a session opened and its position among that day's sessions, derived from the group's reset archive. Three older numbering families survive in old text and are not renumbered. | [BACKLOG_FORMAT.md §3](BACKLOG_FORMAT.md#the-session-identifier) |
| **stale reference** | code, rules | A name-shaped token in an entry's body that resolves to no entry in either backlog file; reported, never refused, and an upper bound on real breakage. | [BACKLOG_FORMAT.md §8](BACKLOG_FORMAT.md#8-the-check) |
| **generated block** | code, rules | The span of ROADMAP.md between the `generated by` markers, rendered from ADR front matter and the board by `build.py --roadmap`; a hand edit inside it is refused by the checker. | [ROADMAP.md — Status by axis](../ROADMAP.md#status-by-axis) |
| **Observed / Inferred** | code, rules | The split every claim about cause has to make: facts anchored to a log, a file or a measurement, and the hypothesis with the check that would confirm it. An unverified inference is a hypothesis, whatever grammar it is written in. | [protocol-13-public.md §11 — Evidence Discipline](../protocol-13-public.md#evidence-discipline), [BACKLOG_FORMAT.md §1](BACKLOG_FORMAT.md#1-the-entry) |

## People, agents, and how they work together

| Term | Met in | Meaning | Defined in |
|---|---|---|---|
| **Protocol 13** | rules | The operating agreement for a team of one human coordinator, one execute agent and one review agent: roles, interaction patterns, communication norms and the decision-artifact layers. | [protocol-13-public.md](../protocol-13-public.md) |
| **Human Coordinator** | rules | The decision-maker: provides direction, approves scope, relays between agents, resolves disagreements. | [protocol-13-public.md §2](../protocol-13-public.md#human-coordinator) |
| **Execute Agent** | rules | Writes code, runs tests, commits; decides *how* given a clear *what*; never approves its own work. | [protocol-13-public.md §2](../protocol-13-public.md#execute-agent) |
| **Review Agent** | rules | Writes design rationale and reviews outcomes; works at the level of *why*; flags what is wrong and leaves *how* to the Execute Agent. | [protocol-13-public.md §2](../protocol-13-public.md#review-agent) |
| **explicit action (DDA)** | rules | Work starts only after an explicit action verb; "agree", "fine", "makes sense" are not authorisation, and ambiguous consent is no consent. | [protocol-13-public.md §11](../protocol-13-public.md#explicit-action-required-dda) |
| **interaction pattern (A–E)** | rules | The five named ways the three participants exchange work: collaborative design, standard execution, direct dialogue, automated @mention routing, and the external agent's monitoring loop. | [protocol-13-public.md §3](../protocol-13-public.md#3-interaction-patterns) |
| **sprint log** | rules | Temporary working memory for the current team in `sprint-logs/`, four sections (design rationale, implementation notes, review findings, decisions made), extracted to ADRs and archived. | [protocol-13-public.md §5](../protocol-13-public.md#layer-2--sprint-logs) |
| **agent** | code, rules, screen | An embedded LLM participant that runs on one node with its own storage under `~/.dpc/agents/<id>/`, tools, memory and permission profile; addressed by display name. | [agent/DPC_AGENT_GUIDE.md](agent/DPC_AGENT_GUIDE.md#architecture) |
| **agent profile** | code, rules, screen | The per-agent block in `privacy_rules.json` that decides which tools the agent may use and which paths it may reach; an agent's own profile beats the global `dpc_agent` block. | [agent/DPC_AGENT_GUIDE.md — Tool Access Control](agent/DPC_AGENT_GUIDE.md#tool-access-control) |
| **sandbox** | code, rules, screen | The directory an agent may read and write by default, `~/.dpc/agents/<id>/`; everything else needs an extended path. | [agent/DPC_AGENT_GUIDE.md — Extended Sandbox Paths](agent/DPC_AGENT_GUIDE.md#extended-sandbox-paths) |
| **extended sandbox path** | code, rules, screen | A directory outside the sandbox granted to an agent as read-only or read-write in its profile. | [agent/DPC_AGENT_GUIDE.md — Extended Sandbox Paths](agent/DPC_AGENT_GUIDE.md#extended-sandbox-paths) |
| **shell tier (0/1/2)** | code, rules, screen | The three-way classification of a `run_shell` command: Tier 0 runs at once, Tier 1 waits for a person's approval in the chat, Tier 2 is hard-blocked and not overridable by config or agent. Not the same word as a connection tier. | [decisions/030 — Tier Model](decisions/030-run-shell-safety-guardrails.md#tier-model) |
| **CDN refusal** | code, rules | A record that a page tried to reach a host and the route gate refused it — either because the site's CDN manifest does not name that host (`unlisted`) or on method or navigation before the manifest is read (`blocked`) — written per agent to `~/.dpc/agents/<id>/web_cdn_refusals.json`. Evidence only: the gate reads the manifest and never the refusals, and a host becomes allowed solely when a human promotes it. Deliberately not called an *observation*; that word is the board's shelf. | [decisions/028 §3](decisions/028-agent-web-auth-cookie-sharing.md#3-the-route-gate-now-constrains-the-destination-not-only-the-initiator) |
| **CDN manifest** | code, rules | `~/.dpc/web_cdn_manifest.json`, node level and outside every agent's sandbox: per site, the exact hosts its pages may pull subresources from. The authority the route gate reads, grown only by a human promoting CDN refusals into it. | [decisions/028 §3](decisions/028-agent-web-auth-cookie-sharing.md#3-the-route-gate-now-constrains-the-destination-not-only-the-initiator) |
| **external agent** | code, rules | A second LLM session running outside DPC — Claude Code or any harness — that reads a conversation from disk and posts back over the local WebSocket API through the bridge scripts. | [agent/CC_INTEGRATION_GUIDE.md](agent/CC_INTEGRATION_GUIDE.md#what-the-external-agent-sees-what-it-does) |
| **tag** | code, rules, screen | The name an external agent answers to in a group, registered per node in Group Settings and stored in the group's metadata; in a 1:1 chat the configured display name is the only identity. | [agent/CC_INTEGRATION_GUIDE.md — Which name it answers to](agent/CC_INTEGRATION_GUIDE.md#which-name-it-answers-to) |
| **bridge** | code, rules | One of the two helper scripts an external agent uses: the agent-chat bridge for 1:1 chats and the group bridge for groups. | [agent/CC_INTEGRATION_GUIDE.md — The bridge scripts](agent/CC_INTEGRATION_GUIDE.md#the-bridge-scripts) |
| **mention** | code, rules, screen | `@Name` in a message routes it to that agent; `@all` fans out to every agent in the group and is human-only; fenced code is stripped before matching. | [GROUP_CHAT.md — Mention routing](GROUP_CHAT.md#mention-routing) |
| **reasoning effort** | code, rules, screen, wire | One word from the shared scale `off / low / medium / high / max` that a caller asks a model in. A provider with an **effort channel** folds the word onto a **rung** of its own ladder; a provider without one refuses every word, `off` included, because a class that cannot ask for thinking cannot say no to it either. The scale is two constants on purpose: `off` is deliberately not a member of `REASONING_EFFORTS`, since it is not an amount of thinking, and code that iterates the levels must not offer it as one. | [`providers/base.py` `REASONING_EFFORTS` / `REASONING_OFF`](../dpc-client/core/dpc_client_core/providers/base.py), [specs/dptp_v1.md §3.4](../specs/dptp_v1.md#34-remote-ai-inference) |
| **provider alias** | code, rules, screen, wire | The name of one configured model endpoint in `~/.dpc/providers.json`; agents, chats and roles name a provider by its alias. | [CLAUDE.md — AI Providers](../CLAUDE.md#ai-providers) |

## Memory, knowledge, and sleep

| Term | Met in | Meaning | Defined in |
|---|---|---|---|
| **Active Recall** | code, rules, screen | Cross-layer retrieval that injects the most relevant knowledge into an agent's prompt without an explicit tool call: FAISS and BM25 fused, budget-aware. | [decisions/010 — Embeddings Active Recall](decisions/010-agent-memory-architecture.md#component-2-embeddings-active-recall--cross-layer-retrieval) |
| **memory layers (L1–L7)** | code, rules | The seven layers agent memory spans, from L1 strategy documents through L5 agent-written knowledge and L6 human-committed knowledge to L7 the human; retrieval priority is L6 > L1 > L5 > L2-docs. | [decisions/010 — Layer Connection Map](decisions/010-agent-memory-architecture.md#layer-connection-map) |
| **knowledge commit** | code, rules, screen, wire | A structured, attributed, versioned unit of knowledge extracted from a conversation, proposed, reviewed, voted on and committed; the human owns the extraction. | [KNOWLEDGE_ARCHITECTURE.md §6](KNOWLEDGE_ARCHITECTURE.md#6-knowledge-commit-protocol), [protocol-13-public.md §6](../protocol-13-public.md#6-knowledge-architecture) |
| **consensus / devil's advocate** | code, rules, screen, wire | Multi-party voting on a knowledge commit, counted over the participants the proposal names — never over the votes that arrived; with three or more participants one is required to dissent. A participant that cannot judge answers `abstain` with a reason, and a deadline never approves. | [KNOWLEDGE_ARCHITECTURE.md §4.3](KNOWLEDGE_ARCHITECTURE.md#43-consensus-with-required-dissent) |
| **sleep consolidation** | code, rules, screen | An on-demand pipeline that reads the previous sessions' archives one at a time, extracts findings, writes them into the knowledge graph and produces a morning brief; it replaced the background "consciousness" worker and the autonomous evolution loop. | [decisions/014](decisions/014-sleep-consolidation-architecture.md), [protocol-13-public.md §7](../protocol-13-public.md#7-between-sessions) |
| **morning brief** | code, rules | The output of a sleep cycle that the next session loads as context; in a group, each agent posts its own brief into the chat. | [decisions/014](decisions/014-sleep-consolidation-architecture.md), [GROUP_CHAT.md — Group Sleep](GROUP_CHAT.md#group-sleep--morning-briefs) |
| **compaction** | code, rules, screen | Summarising aged tool results inside an agent run once context usage crosses a threshold, so a long run keeps fitting; it shrinks tool rounds, not the conversation. | [decisions/033](decisions/033-agent-tool-loop-llm-compaction.md#decision) |
| **skill** | code, rules, screen, wire | A static `SKILL.md` strategy file an agent can execute, share with peers and import; imports fork from their origin. | [agent/DPC_AGENT_GUIDE.md — Skills](agent/DPC_AGENT_GUIDE.md#skills--agent-discovery-tools) |
| **knowledge base** | rules | What a user's document collection becomes once the **document pipeline** has read it: one record per unit, carrying the verdict on what was read and how, indexed and citable. Mike's word, 2026-09-22, against *library*, which is taken twice over here — the cryptography sense in this source tree and the user's own books on disk. The contour of the layers beyond the record is dpc-library's `docs/knowledge-flow.md`, which settles nothing until a decision stands on it. | [decisions/043 — Open Questions](decisions/043-one-reader-now-the-module-in-dpc-messenger-later.md#open-questions) |
| **document pipeline** | rules | The stages that turn a source file into records — scan, classify, route, read, record, summary — reading through the one reader `read_document` uses, so a refusal rate is a number about the corpus and not about which of two readers ran. Accepted by Mike on 2026-09-22 as the name of both the stages and the package that will hold them, `document_pipeline`; the pilot runs in dpc-library until the user flow is known. | [decisions/043 — The eventual home](decisions/043-one-reader-now-the-module-in-dpc-messenger-later.md#the-eventual-home) |

## Nodes, peers, and the wire

| Term | Met in | Meaning | Defined in |
|---|---|---|---|
| **node** | code, rules, screen, wire | One running D-PC client with its own cryptographic identity: a human and their agents on one machine. Not the XML or DOM sense of the word used in other codebases. | [specs/dptp_v1.md §4](../specs/dptp_v1.md#4-node-identity-system) |
| **node_id** | code, rules, screen, wire | `dpc-node-` plus the first 32 hex characters of the SHA-256 of the node's RSA public key; the certificate's Common Name and the key to everything that names a node. | [specs/dptp_v1.md §4](../specs/dptp_v1.md#node-id-format) |
| **peer** | code, rules, screen, wire | Another node this node talks to over DPTP; its certificate is stored only after its key re-hashes to its claimed `node_id`. | [specs/dptp_v1.md §4](../specs/dptp_v1.md#identity-storage) |
| **DPTP** | code, rules, wire | D-PC Transfer Protocol: a 10-byte ASCII length header plus a JSON payload, carried over TLS, WebRTC data channels or DTLS. | [specs/dptp_v1.md §1–2](../specs/dptp_v1.md#1-overview) |
| **HELLO** | code, rules, wire | The first DPTP message on a connection, carrying the peer's `node_id` and display name; both sides send one. | [specs/dptp_v1.md §3.1](../specs/dptp_v1.md#31-connection-establishment) |
| **connection tier (1–6)** | code, rules | One of six strategies the orchestrator tries in order — IPv6 direct, IPv4 direct, Hub WebRTC, UDP hole punching, volunteer relay, gossip store-and-forward. "Complete" in the roadmap means the code shipped, not that the tier carries traffic. | [CLAUDE.md — Connection Types](../CLAUDE.md#connection-types-6-tier-fallback-hierarchy---v0101), [ROADMAP.md — Decentralized Infrastructure](../ROADMAP.md#decentralized-infrastructure-v095---v0102) |
| **star topology** | rules | The live shape of the three-node stand: every link goes through one node because host firewalls block inbound elsewhere; a configuration of a real user, not a defect to fix by opening ports. | [ROADMAP.md — Decentralized Infrastructure](../ROADMAP.md#decentralized-infrastructure-v095---v0102) |
| **peer cache** | code, rules | `~/.dpc/peer_cache.json`, the last known address of each peer; what actually carries traffic when the DHT is empty. | [ROADMAP.md — Decentralized Infrastructure](../ROADMAP.md#decentralized-infrastructure-v095---v0102) |
| **DHT / seed nodes** | code, rules, screen | The Kademlia table a node uses to find peers, entered only through the `[dht] seed_nodes` config line; empty by default. | [CONFIGURATION.md `[dht]`](CONFIGURATION.md#dht) |
| **Hub** | code, rules, screen | The optional federation server: OAuth login and WebRTC signalling only, never message routing or storage; every direct path works without it. | [CLAUDE.md — Key Components](../CLAUDE.md#key-components), [specs/hub_api_v1.md](../specs/hub_api_v1.md) |
| **firewall / privacy rules** | code, rules, screen | `~/.dpc/privacy_rules.json`: what each peer or group may read of this node's context, whether compute and transcription are shared, what agents may use; hot-reloaded on save. | [CLAUDE.md — Context Firewall Rules](../CLAUDE.md#context-firewall-rules) |
| **transcription send-to** | code, rules, screen | The outgoing half of transcription sharing: `transcription.send_to_nodes` / `send_to_groups` in `privacy_rules.json` name the peers this node may hand its **own** audio to when local transcription fails. The opposite of `allow_nodes`, which lets peers use our Whisper; empty means never. Proposed 2026-09-07 with the fallback step — the vocabulary had a word for serving and none for sending. | [CLAUDE.md — Context Firewall Rules](../CLAUDE.md#context-firewall-rules) |
| **remote inference** | code, rules, screen, wire | Running a model on a trusted peer's hardware over the encrypted P2P channel, seen from the side that asks: the requester picks the peer as compute host, and the host's firewall decides whether to serve. The host's side of the same exchange is **Inference Sharing**; "compute sharing" was that tab's name until 2026-09-14 and is kept only by the `compute.*` config keys. | [REMOTE_INFERENCE.md — Overview](REMOTE_INFERENCE.md#overview) |
| **Inference Sharing** | code, rules, screen | The firewall tab and the door that share this node's models with peers (ADR-041): which aliases are served, who may call them, who calls for free, at what tariff, and which models the door accepts. Named "Compute Sharing" until 2026-09-14 (Mike's call: the old name suggested lending a GPU, while what crosses is inference on the host's own model); the config section keeps the name `compute` until the key rename is decided. | [REMOTE_INFERENCE.md — Enabling Inference Sharing](REMOTE_INFERENCE.md#enabling-inference-sharing) |
| **gateway / IDE door** | code, rules, screen | The OpenAI- and Anthropic-compatible HTTP surface this client serves on the consumer's own loopback (ADR-041 D1), so that any tool on this machine — Continue, Cursor, Claude Code, a `curl` line — reaches this node's aliases, and its peers' under a `remote:<node_id>:<alias>` name. **IDE door** is what the Inference Sharing tab's block calls it, and the door is the machine's rather than an editor's: an IDE is the common caller, not the definition. The **peer door** is a different door — DPTP on 8888, where another *node* asks — and the two switches over them do not overlap: `compute.enabled` closes both, `[gateway] enabled` closes this one alone, and neither governs what this node *asks* for, so a node that shares nothing still calls its peers through its own gateway. | [decisions/041 D1](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d1--the-openai-surface-is-a-component-of-the-dpc-client-on-the-consumers-loopback) |
| **node ledger** | code, rules | `~/.dpc/ledger/usage-YYYY-MM.jsonl`: the node's own record of every model call it made or served — an agent's, a peer's or a gateway client's — kept by the node because the card and the vendor key are the node's. Monthly files, append-only, never rotated: a financial record does not lose rows on a schedule. | [decisions/041 D3](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called) |
| **usage row** | code, rules, wire | One line of the node ledger: who called (`caller`, **caller_kind**), what ran (`alias`, `model`, **route**), what it took, whose counts those are (**counts_source**), and what it cost — priced once, at the moment of the call, by the node that made it, and never re-derived. The columns added through 2026-09-14/15 carry the rest: **served_by**, the host of a call this node only consumed; **peer_proved** with `peer_connection_type`, whether the far end's key was proved and over which tier; **output_includes_thinking** and **thinking_source**, what is inside the counts and where the split came from; **served_effort**, the rung the call actually ran at; and the **tariff** group (`tariff_in`, `tariff_out`, `tariff_currency`, `tariff_at`, `tariff_amount`), what the owner charged for it. | [decisions/041 D3](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called) |
| **caller_kind / route** | code, rules | The two columns of a **usage row** that together say whose call it was and where it ran: `caller_kind` is `agent`, `peer` or `gateway` — who asked — and `route` is `local` or `peer` — whose engine answered. There is no `gateway` route: the gateway is a caller, never a destination. The pair is what the ledger reads itself by (`node_ledger.usage_by_role`): `route=local` with `caller_kind=peer` is what this node *served*, `route=peer` is what it *consumed*, and the rest is its *own*. A value outside either vocabulary is refused at write time rather than written, because a row nothing can read is not a record. | [decisions/041 D3](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called) |
| **own / burn** | code, screen | Two readings of the same `route=local` rows, kept apart because they answer two questions. `own_rows` is `route=local` minus `caller_kind=peer` — this node's own consumption on its own key, which the usage tab titles "Own calls (peers excluded)". `burn_rows`, named `owner_rows` until 2026-09-15 and renamed because one letter is not a distinction, is *every* `route=local` row, the calls served to peers included: this node's dollars whoever asked, which is what a burn reader wants in place of the vendor's own usage log. `served_rows` is the difference between the two, and a `gateway` caller is on neither side of it — the gateway door is local, so its client is this node's own user. | [`node_ledger.py`](../dpc-client/core/dpc_client_core/node_ledger.py) |
| **engine time / round trip** | code, screen | The same `duration_s` read from the two sides of one call, named apart because it is not one quantity: **engine time** is this node's own provider call with the engine's warm-up inside it, and **round trip** is this node's wait for a peer's answer, with the wire and the host's queue inside that. The usage tab prints the first over the Served and Own lists and the second over Consumed (`durationLabel`). Measured 2026-09-14 over 28 calls on the two-node LAN/VPN pair: guest minus host had a median of about 1.0 s, 27 of the 28 fell inside 0.92–1.28 s, and the one outlier of 7.3 s is placed outside the host process by the host's own log. Two numbers, and no row carries both. | [`inferenceUsage.ts` `durationLabel`](../dpc-client/ui/src/lib/components/inferenceUsage.ts) |
| **counts_source** | code, rules | Where a **usage row**'s token counts came from, as the writer of that row knows it: `engine` — the numbers were received, from the engine this node called or from a host's answer on the wire, and this node counted nothing; `ours` — this node counted the text itself. A guest's `engine` is therefore not an assertion that an engine counted: a host that counted the text itself writes `ours` on its own row and sends the same numbers, which the guest records as `engine` (open card `TWO-NODES-WRITE-THE-SAME-TOKEN-COUNT-UNDER-TWO-DIFFERENT-WORDS`). It says where the numbers came from and never what is inside them — that is **output_includes_thinking** — and an exact total can hold an estimated split, which is what **thinking_source** is for. | [decisions/041 D3](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called) |
| **output_includes_thinking** | code, rules, wire | `includes` / `excludes` / `unknown`: whether a row's output count already holds the reasoning count, as declared by the node that produced the count rather than assumed from a convention. The word is what sets the billable output, and ADR-041 states the formula: `includes` → `tariff_out × completion_tokens`; `excludes` → `tariff_out × (completion_tokens + thinking_tokens)`, reasoning being billable output at `tariff_out` with no rate of its own; `unknown` → not billed, analytics only — the state of every row written up to that decision. A row written before the column reads as `unknown`. Under `includes` the reasoning is inside the output and never larger than it; a count that overflowed is clamped to the completion and marked `estimated`, so the record of a call that was made is kept rather than refused. | [decisions/041 D3, amendment 2026-09-13](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called), [specs/dptp_v1.md §3.4](../specs/dptp_v1.md#34-remote-ai-inference) |
| **thinking_source** | code, wire | Where a row's `thinking_tokens` came from: `engine`, the host's vendor reported the reasoning split, or `estimated`, the host derived it from the reasoning text, which is what a build that reports no split sends. Null is silence — a node that said nothing, an older row, or a call with no reasoning — and is never a claim that an engine counted. Provenance, not arithmetic: the counts beside it can be exact while the split inside them is a guess, and a **tariff** is charged on the prompt and output counts either way. | [specs/dptp_v1.md §3.4](../specs/dptp_v1.md#34-remote-ai-inference) |
| **peer_proved** | code | On a **usage row**, whether the node at the other end of the call had its key proved on the connection the call travelled over — the line ADR-041 D2 draws for serving peer inference at all. `True` means one of two checks ran: inbound, the certificate's CN is the claimed `node_id`, its public key hashes to that `node_id`, and an RSA-PSS signature over a nonce this node chose verifies under it; outbound, the same CN and key hash over a TLS session that has already made the far end prove it holds the private half. `False` is a tier that runs neither, where the name is the Hub's assertion, this node's own intention or an envelope field. Null means there is no far end to prove — an agent or a gateway client on this machine is not reached over a tier — and is what a row written before the column reads as. `peer_connection_type` beside it names the tier the answer came from. | [decisions/041 D2](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d2--authorised-on-a-connection-whose-peer-key-has-been-proved) |
| **cost_usd** | code, rules, wire | What a call cost the node that ran it, in USD: a vendor's price computed once at the moment of the call, or 0 by construction for a local model; `null` on a guest's row, because the guest ran nothing and prices nothing. | [decisions/041 D3](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called) |
| **billing** | code, rules, wire | Which pool a call's cost belongs to — `subscription` or `pay_per_use` — as `dpc_agent/pricing.py` classifies the alias and model; a label on the usage row, not an amount. | [decisions/041 D3](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called) |
| **served_effort** | code, rules, wire | The usage-row column and `REMOTE_INFERENCE_RESPONSE` field naming the **reasoning effort** the host actually ran a call at — the rung its provider reports having sent, where the provider reports one, and otherwise the guest's word clamped to the host's cap or the alias's configured word. Null means no word describes the call — the alias has no **effort channel**, or its host could not read its own configured word — and is never the same as `off`. Distinct from the word the caller asked for and from the alias's configured word. | [specs/dptp_v1.md §3.4](../specs/dptp_v1.md#34-remote-ai-inference) |
| **effort channel** | code, rules, wire | Whether a provider class can forward a **reasoning effort** word to its engine at all, declared by the class itself (`AIProvider.reasoning_words_served`, empty and fail-closed by default) and read by both doors from that one declaration. llama.cpp (`chat_template_kwargs`), DeepSeek (`reasoning_effort`) and Ollama (`think`) forward the ladder; Z.AI forwards only `off`; the other eight — Anthropic, the OpenAI-compatible provider, Gemini, GigaChat, GitHub Models, DpcAgent, RemotePeer and LocalWhisper — forward none, three of them naming `reasoning_effort` in a signature and saying in the code that they do not send it. What `served_effort` reports on, and why an alias with no channel offers an empty `reasoning_words` menu row and refuses every word, `off` included. | [decisions/041 D4, amendment 2026-09-14](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d4--shipping-order-and-the-colleague-is-served-at-step-4), [specs/dptp_v1.md §3.4](../specs/dptp_v1.md#34-remote-ai-inference) |
| **rung** | code, rules, screen, wire | One step of an alias's own reasoning ladder, as against the shared-scale word a caller asks in (**reasoning effort**). `_rung_of` answers what a word names on one alias: a provider whose ladder is its model's own answers for itself, the rest stand on the shared scale, and a word that reaches no rung is refused by name with the words the alias does accept. The distinction is why `served_effort` reports a rung and not the word that asked for it — an entry point may run a rung the configuration does not name. | [specs/dptp_v1.md §3.4](../specs/dptp_v1.md#34-remote-ai-inference) |
| **tariff** | code, rules, screen, wire | The rate an owner declares for a shared alias — per 1M input and per 1M output tokens, in the node's currency, dated — applied once at the moment of a call and written into both nodes' usage rows as the applied values. `null` means no tariff was declared (a gift), `0` means declared free, `>0` means paid. Distinct from `cost_usd` (what the call cost the host) and from `charge`, which is reserved for the act of settlement. | [decisions/041 D3, amendment 2026-09-10](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called) |
| **unpriced** | code, rules, screen | A **usage row** whose `cost_usd` is null: a call nobody priced. The ledger's readers count such a row rather than summing it as zero, and `spent_today` adds nothing for it, so an unpriced row can never fill a ceiling. Not free — a declared `0.0` is a price somebody computed — and not **untariffed**, which is about what a guest was charged rather than what the call cost the node that ran it. Every `route=peer` row is unpriced by construction: the guest ran nothing and prices nothing. | [decisions/041 D3](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called), [`node_ledger.py`](../dpc-client/core/dpc_client_core/node_ledger.py) |
| **untariffed** | code, rules | A served call for which the host declared no **tariff** — a gift. Counted, never summed as a zero, because a missing tariff is not a declared free one; the usage tab marks a group `gift` when every row in it is untariffed. The sister state of **unpriced** on the other side of the same row: unpriced is what the call cost the node that ran it, untariffed is what the guest was charged for it. | [decisions/041 D3, amendment 2026-09-10](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called), [`node_ledger.py`](../dpc-client/core/dpc_client_core/node_ledger.py) |
| **unpriceable** | code, rules, screen | A **tariff** was declared and the amount could not be computed from the row's own counts. The ledger counts the row (`tariff_unpriceable`) instead of adding a zero, and the usage tab shows it as an `N unpriceable` badge with the "counted, not summed" note that stands on it. Distinct from **untariffed**, where there was no rate to apply, and from **unrated**, which refuses a call before it runs rather than describing one that did. | [decisions/041 D3](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called), [`node_ledger.py`](../dpc-client/core/dpc_client_core/node_ledger.py) |
| **unrated** | code, rules, wire | A vendor alias whose model `dpc_agent/pricing.py` has no rate for. What such a call spends cannot be counted against its ceiling in `compute.vendor_quotas` — the meter is absent rather than slow — so both doors refuse it rather than serve it against a ceiling that would read `0.00` for ever: the word `unrated` on the wire, and 503 with no `Retry-After` at the guest. The repair is a rate on the host and never waiting, which is why it was split off `insufficient_quota` on 2026-09-15. Not an **unpriced** row: that is the record of a call that ran. | [specs/dptp_v1.md §3.4](../specs/dptp_v1.md#34-remote-ai-inference), [decisions/041 D5](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d5--api-backed-models-are-shareable-and-the-quota-is-a-financial-control) |
| **served_by** | code, rules | The node that ran a call this node only consumed: the host's `node_id` on a `route=peer` usage row, written beside the tariff group because the two answer one question together — what the call was charged and by whom. Absent, never null, on a row this node ran itself, where there is no other node to name; an `alias` is unique only under its host, so the reader keys a consumed row `remote:<served_by>:<alias>`. A `route=peer` row written before the column names no host and keys `remote:?:<alias>` (`node_ledger.UNKNOWN_HOST`), shown as "host not recorded": a node id is `dpc-node-<32 hex>`, so `?` is a name no host can have and the bucket collides with none. Keyed by the bare alias, as such a row was until 2026-09-15, it carried the very string a local alias of that name uses, and a node that both serves and consumes one alias name folded the two into one line. | [decisions/041 D3, amendment 2026-09-14](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called) |
| **`remote:?:<alias>`** | code, screen | The consumed key of a **usage row** written before **served_by** existed. A `route=peer` row is keyed `remote:<served_by>:<alias>`, and a row that names no host takes `node_ledger.UNKNOWN_HOST`, `?`, in the host's place: a node id is `dpc-node-<32 hex>`, so `?` is a name no host can have and the bucket it keys collides with none. The usage tab reads it as a `host not recorded` badge rather than naming a host it does not know. Keyed by the bare alias, as such a row was until 2026-09-15, it carried the very string a local alias of that name uses. | [`node_ledger.py` `consumed_key`](../dpc-client/core/dpc_client_core/node_ledger.py) |
| **free_nodes / free_groups** | code, rules | The subset of the allowed nodes and groups that a shared alias is served to at a tariff of 0 — free for these, the tariff for everyone else allowed. `allow_nodes` / `allow_groups` remain the door; the pair mirrors the transcription block's `allow` / `send_to`. | [decisions/041 D3, amendment 2026-09-10](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called) |
| **compute.currency** | code, rules, wire | The ISO 4217 unit a node declares its tariffs in. A usage row freezes it into itself (`tariff_currency`), so rows outlive a changed configuration; the protocol picks no currency, and parity between two nodes' units is their own agreement. | [decisions/041 D3, amendment 2026-09-10](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d3--one-usage-row-per-model-call-on-the-node-that-ran-it-carrying-who-called) |
| **serving lists** (`serving_local`, `serving_vendor`, `vendor_quotas`) | code, rules, screen | What a node serves to peers and to its own gateway: local aliases, bounded by the card, and vendor aliases, bounded by a USD-per-day ceiling per caller in `vendor_quotas`; an alias in neither list is not served. Every alias in `serving_local` is served over P2P since 2026-09-18 (amendment); the first entry is what a peer request naming no provider is served from. `serving_alias` is the deprecated single form, read as `serving_local = [alias]`. Standing on a list is necessary and not sufficient: a vendor alias no rate table can price is refused rather than served for free, because a ceiling summed from rows that all cost 0.00 could never bind; and a transcription-only alias is left off the chat menu and refused at the door with `model_not_found` saying it transcribes, on this node's own lists as on a peer's rows. | [decisions/041 D5, amendment 2026-09-10](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d5--api-backed-models-are-shareable-and-the-quota-is-a-financial-control) |
| **menu row / menu** | code, rules, screen, wire | One `providers` entry of a PROVIDERS_RESPONSE, resolved by the host for one recipient: `alias`, `model`, the `supports_*` flags and **serves_images_with_tools**, `reasoning_words`, `reasoning_default`, the **tariff** and the `settings` block. The **menu** is the set of rows a host would send that peer (`service.menu_for_peer`), which is not the set a door offers: a transcription-only alias stands on the menu behind its own permission and is refused at the chat door with `model_not_found` saying it transcribes (`608cd4fa`). A guest reads a row fail-closed — an absent key is the host's silence, never a default — and the row, not the guest's own table, is the dictionary a **rung** is checked against. | [specs/dptp_v1.md §3.5](../specs/dptp_v1.md#35-provider-discovery), [`service.py` `menu_for_peer`](../dpc-client/core/dpc_client_core/service.py) |
| **serves_images_with_tools** | code, rules, wire | The **menu row** field saying the host serves a request whose `messages` hold `image` blocks beside `tools` on that alias — the pictures in their turns, the tools called. It names the other kind of row flag: a `supports_*` flag (`supports_vision`, `supports_tools`) says what the provider can do, a `serves_*` flag says what the node's route will serve for a request of one shape, and the two are not interchangeable. Set from `entry_point_for(provider, tools=True, streaming=False, images=True)`, the predicate the host's gate and `query_messages` ask — never from the two `supports_*` flags multiplied. Fail-closed where `supports_vision` is not: a guest reads it absent as no, because a host that predates it may drop the pictures without a word; a guest refuses the combination with `tools_unsupported` before the round trip, and the host's own refusal stays the gate. Name accepted by Mike, 2026-09-17. | [specs/dptp_v1.md §3.5](../specs/dptp_v1.md#35-provider-discovery), [decisions/041 D4, amendment 2026-09-17](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d4--shipping-order-and-the-colleague-is-served-at-step-4) |
| **unit** | code, rules, wire | The scale a **tariff**'s `in` and `out` rates are quoted in, on the provider row that carries them. `per_1m_tokens` is the only value DPTP v1.7 defines, and a receiver that does not know the word must not price the row rather than guess a scale. | [specs/dptp_v1.md §3.5](../specs/dptp_v1.md#35-provider-discovery) |
| **max_output_tokens** | code, rules, screen, wire | The ceiling on one answer that a call on an alias will actually run at, carried under that one name whatever the provider's own field is called — `num_predict` on Ollama, `max_tokens` on DeepSeek and llama.cpp. The host's own dial, read from the host's configuration and stated so a guest sees what it is choosing, including the settings it cannot change; it is not a ceiling a caller sets. Fail-closed: absent means "not stated", never "no ceiling applies". | [specs/dptp_v1.md §3.5](../specs/dptp_v1.md#35-provider-discovery) |
| **variant** | code, screen, wire | In a **menu row**'s `settings`, the build behind `model` where the host can read one — a GGUF file name, say. Absent is legal and reads "not stated by the host", never a default: the guest's panel prints the line either way, because a dial the host did not state is itself worth reading. ADR-041 D7's older word for the same field is *quantization*, which names one property of a build rather than the build, and is being reworded. | [specs/dptp_v1.md §3.5](../specs/dptp_v1.md#35-provider-discovery) |
| **refusal code** | code, rules, wire | The machine-readable word beside the prose `error` on an error `REMOTE_INFERENCE_RESPONSE`, naming at which of the host's gates the call was refused so that a refusal keeps its reason across the hop. The nine: `identity_unproved` (the connection's key was not proved — see **peer_proved**), `not_allowed` (the host's firewall does not let this peer ask for inference, or not for what it asked), `model_not_found` (the host serves no alias to this peer, or not the one named), `onward_sharing_refused` (the alias is itself somebody else's model, and what is shared is not shared onward), `invalid_value` (a gate that names the guest's own request — an effort word the alias has no **rung** for, or a `request_id` already **in flight** from this peer), `tools_unsupported` (tools sent to an alias with no native tool-calling path, or beside images on a path that cannot carry both — image blocks in `messages` to an alias that cannot see, or the flat `images` field, whose vision entry point takes no tools; see **serves_images_with_tools**), `insufficient_quota` (this guest's daily ceiling on a vendor alias, spent — 429 at the guest, come back tomorrow), `unrated` (a vendor alias `dpc_agent/pricing.py` has no rate for — 503, repaired by adding a rate) and `misconfigured` (the host cannot read its own serving lists, so the class of the alias is unknown — 503, repaired by its owner). The last three were one word until 2026-09-15 and were split while v1.7 is unreleased, because a code is read as an instruction and only the ceiling refills by itself. It rides beside `error` and never replaces it: the code is what a program reads and the text what a person reads. Absent means a failure mid-call rather than a gate, and is never itself a reason; a receiver reads an unknown word exactly as an absent one, because a newer host may name a cause this one has no word for, and no receiver refuses a message over its code. | [specs/dptp_v1.md §3.4](../specs/dptp_v1.md#34-remote-ai-inference) |
| **in flight** | code, wire | A `request_id` a host is serving for one peer right now. The guest mints the id that keys the host's **usage row** and routes its chunks, so only the host can see a reuse; the pair (peer, id) is held unique for a non-empty id only (`if request_id:`) and released in the `finally`, which is to say when the call has ended, answered or failed alike. A reuse while the first call is still in flight is refused `invalid_value` before the queue and before any row, because two calls under one id would cross their chunk streams and collapse two rows into one — the card is `THE-GUEST-CHOOSES-THE-REQUEST-ID-THAT-JOINS-ITS-ROW-TO-THE-HOSTS-AND-THE-HOST-NEVER-CHECKS-IT`. | [specs/dptp_v1.md §3.4](../specs/dptp_v1.md#34-remote-ai-inference) |
| **endpoint_not_served** | code, rules | The gateway's own 404 code for a route it deliberately does not serve — `/v1/embeddings`, `/v1/completions` and Anthropic's legacy `/v1/complete` — answered with a sentence naming the route and the chat routes that are served, rather than aiohttp's bare 404, which a client cannot tell from a wrong port or a door that is down. A door-local word, in the family of `model_not_found` and `card_busy`: no DPTP command learns it, because nothing crosses the wire. | [decisions/041 D1, amendment 2026-09-14](decisions/041-openai-compatible-gateway-as-a-dpc-surface.md#d1--the-openai-surface-is-a-component-of-the-dpc-client-on-the-consumers-loopback) |
| **is_connected** | code | On a peer row of the node status, that a live P2P connection to that peer exists right now. Every such row is built from `p2p_manager.peers`, which holds the connections that exist, so the field says what the list already meant rather than leaving each reader to infer it. Distinct from known — the caller picker offers every node the application holds a name for, connected first — and from allowed, which is the **firewall / privacy rules** answer and no part of this field. | [`service.py` `get_status`](../dpc-client/core/dpc_client_core/service.py) |
| **message signing / preimage** | code, rules, wire | A message signature covers a canonical preimage of ten fields in a fixed order, tagged `dptp-msg-v1`; signed by the author at send time, never re-signed by a receiver. | [specs/dptp_v1.md §4.1](../specs/dptp_v1.md#41-message-signing), [decisions/036](decisions/036-message-authenticity-signed-at-origin.md#decision) |
| **verdict (verified / unverified / legacy)** | code, rules, wire | What a receiver concludes about an arriving record: hash and signature check against a cached certificate; the same except the certificate is not yet cached; or no signature this receiver can recompute. The verdict is the receiver's, and absence of a signature is never acceptance. | [specs/dptp_v1.md §4.2](../specs/dptp_v1.md#42-verdicts-on-receipt) |

## Groups

| Term | Met in | Meaning | Defined in |
|---|---|---|---|
| **group** | code, rules, screen, wire | A conversation fanned out to N participants under a `group_id`; each member keeps its own copy of metadata and history, reconciled on connect. | [GROUP_CHAT.md — Architecture](GROUP_CHAT.md#architecture) |
| **roster** | code, rules, screen, wire | The group's `members` (human nodes) plus `agents` and `agent_names` per node, carried in `metadata.json` and converged by `GROUP_SYNC` on version with a content-hash tie-break. | [GROUP_CHAT.md — Data Model](GROUP_CHAT.md#data-model), [decisions/038](decisions/038-group-roster-as-signed-state.md#decision) |
| **per-reader role derivation** | code, rules | In a group with several agents the same message is `assistant` to its author and `user` to everyone else, so roles are derived per reader when the payload is built, not stored. | [GROUP_CHAT.md — Per-Reader Role Derivation](GROUP_CHAT.md#per-reader-role-derivation-adr-031) |
| **history sync** | code, rules, screen, wire | On connect, nodes exchange `{count, hash}` per group and per author, and ship only what differs; disk is the source of truth and `message_id` deduplicates. | [GROUP_CHAT.md — Cross-Node History Sync](GROUP_CHAT.md#cross-node-history-sync) |
| **New Session** | code, rules, screen, wire | A vote to clear a conversation's history; in a group every member must be online and vote yes, silence times out as rejection, and a member gone for good blocks the reset until removed. | [GROUP_CHAT.md — Session Reset](GROUP_CHAT.md#session-reset) |
